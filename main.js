// Working on user ability to deposit money into slot machine

const prompt = require("prompt-sync")();

const deposit = () => {
    const depositAmount = prompt("Enter a deposit amount: ");
};

// Testing w/ node
deposit();